import { EmptyState } from "@/components/empty-state"

export function RecentActivity() {
  return <EmptyState title="Nenhuma atividade registrada" description="As ações reais aparecerão aqui conforme o sistema for usado." />
}
