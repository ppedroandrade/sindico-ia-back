import { EmptyState } from "@/components/empty-state"

export function AdminConversations() {
  return <EmptyState title="Nenhuma conversa registrada" description="As conversas reais aparecerão aqui quando a IA for utilizada." />
}
