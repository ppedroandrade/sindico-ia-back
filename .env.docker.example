# Gere valores fortes e únicos antes de rodar (nunca reutilize os exemplos abaixo):
#   openssl rand -base64 24   -> POSTGRES_PASSWORD
#   openssl rand -base64 32   -> JWT_SECRET
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
POSTGRES_DB=sindico
POSTGRES_PORT=5432

JWT_SECRET=
JWT_EXPIRES_IN=1d

CORS_ORIGIN=http://localhost:3000
BACK_PORT=3002
FRONT_PORT=3000
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://back:3002
